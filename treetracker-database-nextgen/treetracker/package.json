{
  "name": "treetracker-database-nextgen",
  "version": "1.0.0",
  "description": "db-migrate-driven schema for the local Treetracker DB; first migration is the online dev public-schema baseline",
  "scripts": {
    "migrate:up": "db-migrate up -e local -t nextgen_migrations",
    "migrate:down": "db-migrate down -e local -t nextgen_migrations",
    "migrate:reset": "db-migrate reset -e local -t nextgen_migrations"
  },
  "dependencies": {
    "db-migrate": "^0.11.14",
    "db-migrate-pg": "^1.5.2"
  }
}
